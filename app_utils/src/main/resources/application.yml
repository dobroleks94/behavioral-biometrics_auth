initial-user:
  login: oleksii_dobroshtan
  password: dobroshtan_Password94

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/diploma_masters_DB?serverTimezone=Europe/Kiev
    username: dobroshtan
    password: Dobroshtan4444
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  config:
    activate:
      on-profile: testing_mysql_db
---
spring:
  datasource:
    url: jdbc:h2:file:/home/dobroshtan/h2db/diploma_masters_db_testing
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  config:
    activate:
      on-profile: testing
---
spring:
  datasource:
    url: jdbc:h2:file:/home/dobroshtan/h2db/diploma_masters_db
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
