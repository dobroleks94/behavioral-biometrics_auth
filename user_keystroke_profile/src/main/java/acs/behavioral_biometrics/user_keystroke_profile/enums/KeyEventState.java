package acs.behavioral_biometrics.user_keystroke_profile.enums;

/**
 * Describes state when particular key is released (KeyUp) or pressed (KeyDown)
 */
public enum KeyEventState {
    KeyUp, KeyDown
}
